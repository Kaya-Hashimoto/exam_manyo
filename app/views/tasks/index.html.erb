<h1>Task一覧</h1>

<% @tasks.each do |task| %>
  <p>
    <%= task.title %>
    <%= task.content %>
    <%= link_to '詳細', task_path(task) %>
    <%= link_to '編集', edit_task_path(task) %>
    <%= link_to '削除', task_path(task), method: :delete, date: { confirm: "削除していいでですか？"} %>
  </p>
<% end %>
