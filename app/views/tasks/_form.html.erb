<%= form_with(model: @task, local: true) do |form| %>
  <div>
    <%= form.label :title %>：
    <%= form.text_field :title %>
  </div>
  <div>
    <%= form.label :content %>：
    <%= form.text_area :content %>
  </div>
  <%= form.submit '送信' %>
<% end %>
